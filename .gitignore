# 运行时数据（实际在用户数据目录，这里兜底忽略本地副本）
data/
*.log

# 构建产物
build/
dist/
*.spec.bak

# Python
__pycache__/
*.pyc

# 测试产物
tests/out/
tests/*.png
tests/*.html
.pytest_cache/

# 本地测试用 nginx（不入库）
nginx-1.30.4/

# IDE
.idea/
.vscode/
version_info.py

# macOS
.DS_Store

# AOCI agent host integration (machine-bound; do not commit)
.mcp.json
.mcp.json.backup.*
.codex/config.toml
.codex/config.toml.backup.*

# 宿主会话与工作区接入内容（机器绑定：MCP 配置里是本机绝对路径；计划文件每次会话生成）
.zcode/
