# 运行时数据（实际在用户数据目录，这里兜底忽略本地副本）
data/
*.log

# 构建产物
build/
dist/
*.spec.bak

# Python
__pycache__/
*.pyc

# 测试产物
tests/out/
tests/*.png
tests/*.html
.pytest_cache/

# 本地测试用 nginx（不入库）
nginx-1.30.4/

# IDE
.idea/
.vscode/
version_info.py

# macOS
.DS_Store

# AOCI agent host integration (machine-bound; do not commit)
.mcp.json
.mcp.json.backup.*
.codex/config.toml
.codex/config.toml.backup.*

# 宿主会话产物（plan mode 的计划文件等；每次会话生成，不属于仓库内容）
.zcode/plans/
