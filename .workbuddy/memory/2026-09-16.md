# 2026-09-16

## P0+P1 功能包（代理模板 / upstream / 实时指标 / 访问日志 / diff 体系）

用户以 PM 视角选定路线（SSL 明确跳过）后一次交付，共 9 文件约 1600 行：

- **代理场景模板**：添加代理可选 standard / websocket / sse / upload，模板决定追加指令
  （WS：`proxy_http_version 1.1` + Upgrade/Connection 头 + 300s 读超时；SSE：`proxy_buffering off` 等；上传：`client_max_body_size 1024m` 等）。列表按块内特征指令反向嗅探模板并展示标签
- **upstream 负载均衡**：proxymgr 新增 `upstream` 块解析/增删改（round_robin / least_conn / ip_hash，weight/backup/down，extra 参数原样保留），保存按规范形式整块重写写入 http{} 末尾；被代理引用时删除被拒；代理目标填 `http://名称` 即接入，前端 datalist 联想
- **实时指标**：nginxctl 新增 stub_status 检测（同文件向上最近 location 的启发式）、listen 端口解析、urllib 抓取解析；状态栏新增活跃连接/请求（前端两次采样差值算速率）；未配置时一键开启（写入仅允许 127.0.0.1 的状态页，走备份+校验流水线）
- **访问日志**：解析 access_log 指令 + prefix/logs 兜底得到候选路径，deque 有界读尾部 500 行，前端本地关键字过滤；path 参数限制在 prefix/confDir 内防任意文件读取
- **diff 体系**：保存前本地行级 LCS diff 预览（前后缀裁剪 + 超大改动退化整体替换，上下文 2 行）；`nginx -t` 失败解析 errFile/errLine（行尾 `:数字` 锚定，兼容 Windows 盘符），编辑器一键跳行；备份任意两版本 unified diff（difflib）
- 契约纪律：先改 API.md（upstream/metrics/access-log/diff 端点 + errFile/errLine）→ 后端 → 前端；`_proxy_apply` 从代理专用泛化为通用配置写流水线，upstream/stub_status 复用

**验证**：43 项功能测试全过（含裸地址/重名/越界 weight 等拒绝样例）；mock nginx e2e 全链路（重名 409、引用删除 409、metrics 幂等、路径穿越 400/403、预览模式空响应）；本机无真实 nginx，真实 `-t` 校验待 Windows 实机冒烟

**坑与备忘**：
- 本机默认 python3 是 3.8，dict `|` 合并（3.9+）会运行时炸，换 `{**a, "k": v}` 写法
- 单实例锁会把同数据目录的第二个测试实例直接杀掉，多实例本地测试要用不同 NGINX_MANAGER_DEFAULT_DATA_DIR
- 带 --preview 且 settings 已配置时走正常模式（避免预览中配置后丢配置），测预览模式必须用空数据目录
- **AOCI rc12 工具层冲突（未闭环）**：maintain 签发 9 个 code_stale 候选，但 update_entry 网关 Schema 与机器校验矛盾——机器要求 candidate_id+source_sha256 同时携带，网关 Zod 的任何合法形态都无法同时容纳两者（穷举 6 种排列，全部 formal_writes_started=false 零写入）；aoci_report 在 Volumes v1 下 volume_read_only。语义已创作完毕，待服务升级后重新 maintain 提交
