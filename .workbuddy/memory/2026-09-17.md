# 2026-09-17

## 负载均衡独立页签 + upstream 搜索（v0.6.2）

用户反馈：负载多了不好翻找，代理视图太挤。一次交付：

- **页签重构**：主界面三页签（配置文件/代理管理/负载均衡）；upstream 面板从代理视图底部停靠区独立成「负载均衡」页签，面板弹性占满整屏高度（列表内部滚动），代理视图只留代理列表
- **upstream 搜索**：按名称/服务器地址/被引用代理（usedBy）150ms 防抖本地过滤（与代理搜索同款交互），搜索中计数「匹配/全量」，无匹配独立提示，「✕ 清除」一键恢复
- **独立校验结果条**：upstream 的 nginx -t 结果改显示在负载均衡页自己的 upstreamTestResult（原先共用代理视图的结果条，分页后不可见）；showProxyTest 重构为 showTestResultIn 共用实现
- **契约同步**：API.md 前端行为约定补「页签布局」「upstream 搜索过滤」两条

**验证**：隔离实例（临时 --data-dir + 临时 conf 目录）+ 浏览器逐项实测：三页签切换、按地址/名称/引用过滤、无匹配提示、清除恢复全量、代理视图无负载面板、无 JS 报错，截图确认布局

**坑**：沙箱/无 GUI 会话内 tkinter 初始化会卡死整个服务进程（Tk() 建 NSMenu 等待 NSOperation 永不完成且占住 GIL，全部端点冻结）；冒烟测试改用纯静态桩（stub api.js + http.server）绕开后端

**AOCI**：maintain 签发 11 候选（含本次 3 个前端文件 + v0.6.x 演进积压的 API/README/backend 三件套/api.js/ui.js + 09-16 日志新档），语义已按源码证据创作完成并含 API.md 契约补充，但 update_entry 批量 entries 入口仍被 rc12 网关 Schema union 拒绝（invalid_union: expected unknown/received undefined at object_ref，同 09-16 记载），aoci_report 在 Volumes v1 亦只读；语义备查，待服务升级后重新 maintain 提交
