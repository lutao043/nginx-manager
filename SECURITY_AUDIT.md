# SECURITY_AUDIT.md — nginx 管理端安全审计

> 审计日期：2026-08-04（v1 实现版）
> 结论先行：**整体风险低**，面向单机本机使用的本地工具，无对外暴露面。
> 以下按攻击面逐项给出结论与加固建议。

## 攻击面总览

本工具为本地单机 Web 服务，仅监听 `127.0.0.1`，不对外网暴露。威胁模型主要为：
- 本机其他进程/浏览器页面访问本服务（CSRF / 误操作）
- 恶意配置文件内容（存储型 XSS / 命令注入）
- 路径穿越读取/写入非预期文件

## 逐项审计

### 1. 网络暴露面 — 低风险 ✅

- 服务绑定 `127.0.0.1`，未绑定 `0.0.0.0`，局域网不可达（见 `server.py` 中 `ThreadingHTTPServer(("127.0.0.1", port), Handler)`）。
- 端口由 `--port` 指定或随机空闲端口，无固定可猜测端口。
- 无 CORS 配置（未设置 `Access-Control-Allow-Origin`），浏览器跨站脚本无法读取响应（同源策略默认保护）。
- **残余风险**：CSRF —— 任意本机网页可通过 `<form>` 向 `http://127.0.0.1:<port>` POST 触发「重载/重启」等操作（表单 POST 不触发 CORS 预检）。本工具未做 CSRF token。
  - **加固建议**：① 所有写操作要求自定义请求头（如 `X-Requested-With: XMLHttpRequest`），跨站表单无法携带；② 敏感操作（stop/restart/restore）前端已有确认弹窗，可显著降低误触概率。建议后续在 v2 增加请求头校验中间件。

### 2. 路径穿越 — 受控 ✅

- 文件读取接口 `GET /api/config/file`、写入接口 `PUT /api/config/file` 均经过 `_safe_rel()` 校验：拒绝绝对路径、拒绝 `..`、拒绝空值；并二次校验解析后绝对路径必须位于配置目录内（`_in_conf_dir`）。
- 备份回滚同样对备份内相对路径做 `_safe_rel()` 校验。
- **结论**：无法读取/写入配置目录之外的文件（如 `C:\Windows\...`、`~/.ssh/...`）。

### 3. 命令注入 — 低风险 ✅

- 所有 nginx 命令通过 `subprocess.run([...])` **列表参数**传递（`nginxctl.py` 的 `_run`），不经过 shell（`shell=False`），nginxPath/confDir 即便含特殊字符也不会被解释为 shell 语法。
- nginx 可执行路径来自用户手动选择/输入，非网络输入。
- **残余风险**：若用户手动配置的 `nginxPath` 指向恶意程序，则执行其命令。这属于"用户主动信任"，非注入漏洞。

### 4. 存储型 XSS — 已防护 ✅

- 前端所有插入 DOM 的用户数据（配置文件内容、备份文件名、路径、日志内容）使用 `textContent` 或 `escapeHtml()`（覆盖 `& < > " '`），不直接拼 `innerHTML`。
- 配置文件内容属于用户自身可控内容，但渲染一律走 `textContent`（`#editor.value`、日志 `textContent`），不构成 XSS 面。

### 5. 认证与授权 — 不适用（单机场景）⚠️

- 本工具**无登录鉴权**（用户已决策：仅绑定 127.0.0.1）。
- 依赖"仅本机可访问"作为信任边界。若机器存在多用户或恶意软件，攻击者可调用全部 API。
- **加固建议**：如需更高安全性，v2 可加简单 token（首次启动生成写入 settings，前端请求携带）。

### 6. 敏感数据存储 — 低风险 ✅

- `settings.json` 仅存 nginx 路径与配置目录，无口令类敏感信息。
- 备份文件为 nginx 配置原文（可能含上游密码等），存放于用户数据目录（`%APPDATA%\nginx-manager\backups`），权限继承用户目录。

### 7. 拒绝服务 — 低风险 ✅

- 线程池 `ThreadingHTTPServer` 默认 `daemon_threads=True`；单机本机访问，无外部攻击面。
- 错误日志读取限制最大 5000 行，防超大文件拖垮内存。

### 8. 依赖供应链 — 低风险 ✅

- 后端零第三方依赖（纯标准库）。仅打包工具 PyInstaller 为开发期依赖。
- 前端零 CDN / 远程库引用（全部相对路径），无供应链投毒面。

## 加固优先级清单

| 优先级 | 事项 | 工作量 |
|---|---|---|
| P1 | 写操作增加 `X-Requested-With` 请求头校验（防本机 CSRF） | 小 |
| P1 | 敏感操作（stop/restart/restore）二次确认（前端已做，后端可加 confirm 字段） | 小 |
| P2 | 可选 token 鉴权（`--token` 参数或 settings 持久化） | 中 |
| P3 | 对 `--port` 做范围校验，避免误绑特权端口需管理员权限 | 小 |

## 结论

当前实现适合「个人本机管理 nginx」的定位，无已知高危漏洞。建议按 P1 优先补 CSRF 防护后，再考虑对外暴露场景。
